services:
  mongo:
    image: mongo:7
    container_name: jumpstart-mongo
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_DATABASE: jumpstart
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
